/** Vector equilibrium resolvers — re-export shell (SSOT: core). */
export {
  resolveEquilibriumMode,
  resolveActiveNode,
  enrichSystemStateVectorEquilibrium,
} from "../core/vector-equilibrium-core";
