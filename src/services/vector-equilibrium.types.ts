/**
 * Vector Equilibrium types — re-export shell (SSOT: core).
 */
export type {
  EquilibriumMode,
  TopologyNode,
  VectorEquilibriumContext,
  TopologyNodeUiConfig,
} from "../core/vector-equilibrium-types";

export {
  TOPOLOGY_DELTA_CRI_MAX,
  TOPOLOGY_LAMBDA_CRI_MIN,
  TOPOLOGY_KAN_CRI_MAX,
  TOPOLOGY_LI_CRI_MIN,
} from "../core/vector-equilibrium-types";
