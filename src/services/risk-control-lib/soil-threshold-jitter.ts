/** Dynamic soil threshold jitter — thin re-export (SSOT: core/soil-resistance-core). */
export {
  JITTER_MIN_BPS,
  JITTER_MAX_BPS,
  resolveJitteredSoilThresholds,
} from "../../core/soil-resistance-core";
