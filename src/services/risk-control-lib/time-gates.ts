/** HKT tsunami + HL orderbook gap time gates — thin re-export (SSOT: core/soil-resistance-core). */
export {
  TSUNAMI_SHIELD_HKT_START,
  TSUNAMI_SHIELD_HKT_END,
  getHktHour,
  isTsunamiShieldWindow,
  isHlOrderbookGapWindow,
} from "../../core/soil-resistance-core";
