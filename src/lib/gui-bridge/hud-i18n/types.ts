/** HUD locale types — English only. */

export type HudLocale = "en";

export type WeaponTooltip = {
  en: string;
  labelEn: string;
  /** Short defense target for Wiki Hub */
  targetEn: string;
  code: string;
};
