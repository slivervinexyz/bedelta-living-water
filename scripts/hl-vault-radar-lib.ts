/**
 * Hyperliquid Vault Risk Radar — shared fetch + scoring for B2B lead gen.
 * @deprecated Import from `./vault-radar/` directly; this shim preserves legacy paths.
 */

export * from "./vault-radar/index.js";
